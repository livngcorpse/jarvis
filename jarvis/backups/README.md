# Backups Directory

This directory contains backups of files before they are modified by the self-modification system.

Each backup is stored in a subdirectory named with a timestamp of when the backup was created.